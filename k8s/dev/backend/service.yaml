apiVersion: v1
kind: Service
metadata:
  name: pythondeepcheck-backend
  namespace: storefront-devpy
  labels:
    app.kubernetes.io/name: pythondeepcheck-backend
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pythondeepcheck-backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
