apiVersion: v1
kind: Service
metadata:
  name: pythondeepcheck-frontend
  namespace: storefront-dev003
  labels:
    app.kubernetes.io/name: pythondeepcheck-frontend
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pythondeepcheck-frontend
  ports:
    - name: http
      port: 80
      targetPort: 8080
